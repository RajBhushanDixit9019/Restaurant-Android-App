<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="20dp" />
            <padding
                android:left="2dp"
                android:top="2dp"
                android:right="2dp"
                android:bottom="2dp" />
            <!-- Apply slight shadow with transparent color -->
            <solid android:color="#66000000" /> <!-- Semi-transparent black -->
        </shape>
    </item>

    <!-- Main card layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/card_background_color" />
            <corners android:radius="20dp" />
            <stroke android:width="1dp" android:color="@color/card_border_color" />
        </shape>
    </item>
</layer-list>
